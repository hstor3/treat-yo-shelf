gsap.fromTo(".top-logout-btn", { opacity: 0, scale: 0 },
    { opacity: 1, scale: 1, duration: 2.5 });
// home pg
gsap.fromTo(".logo-home", { opacity: 0, y: -25, scale: 0 },
    { opacity: 1, y: 0, scale: 0, duration: 2.5 });

gsap.fromTo(".header-text", { opacity: 0, y: -25, scale: 0 },
    { opacity: 1, y: 0, scale: 1, duration: 2.5 });

gsap.fromTo(".home-login-card", { opacity: 0, y: 100, scale: 0 },
    { opacity: 1, y: 0, scale: 1, duration: 2 });

gsap.fromTo(".home-card", { opacity: 0, y: 100, scale: 0 },
    { opacity: 1, y: 0, scale: 1, duration: 2 });

// login pg
gsap.fromTo(".logo-login", { opacity: 0, y: -25, scale: 0 },
    { opacity: 1, y: 0, scale: 1, duration: 2.5 });

gsap.fromTo(".header-text-login, .login-form, .signup-form", { opacity: 0, y: 50 },
    { opacity: 1, y: 0, duration: 2.5 });

// search 
gsap.fromTo(".searchbar, .header-text-search", { opacity: 0, y: -25, scale: 0 },
    { opacity: 1, y: 0, scale: 1, duration: 2.5 });

gsap.fromTo(".searchCard", { opacity: 0, y: 50, scale: 0 },
    { opacity: 1, y: 0, scale: 1, duration: 2.5 });

// list

// Recommended 